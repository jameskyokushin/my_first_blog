<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :commenter, 'FROM:' %><br />
    <%= f.text_field :commenter %>
  </div>
    <div class="field">
    <%= f.label :body, 'MESSAGES:'%><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit "SEND" %>
  </div>

<% end %>
